name: testing_flutter
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  
  # State Management
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5

  # Firebase (commented out - not configured yet)
  # firebase_core: ^3.15.2
  # cloud_firestore: ^5.6.12

  # UI & Design
  google_fonts: ^6.2.1
  cached_network_image: ^3.3.1
  flutter_staggered_grid_view: ^0.7.0
  
  # Navigation
  go_router: ^14.0.0

  # Local Storage (Community Edition - actively maintained, full cross-platform + WASM web)
  hive_ce: ^2.7.0
  hive_ce_flutter: ^2.2.0

  # Image Picker
  image_picker: ^1.1.2

  # Utilities
  intl: ^0.20.2
  shared_preferences: ^2.2.3
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.9
  riverpod_generator: ^2.4.0

flutter:
  uses-material-design: true
  generate: true

